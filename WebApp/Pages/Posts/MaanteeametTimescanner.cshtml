@page
@using Markdig
@model WebApp.Pages.Posts.MaanteeametTimescanner
@{
    ViewData["Title"] = "Maanteeamet Timescanner";
}

<div class="content-container px-4">
    <div class="mt-3 mb-3">
        <h4 class="fw-semibold">Maanteeamet Timescanner</h4>
    </div>
    <p>
        @{
            var pipeline = new MarkdownPipelineBuilder().UseAdvancedExtensions().Build();
        }
        @Html.Raw(Markdig.Markdown.ToHtml(Model.Content, pipeline))
    </p>
</div>